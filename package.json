{
  "name": "hats-indexer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "sdk:dev": "pnpm --filter hats-ponder-sdk dev",
    "sdk:build": "pnpm --filter hats-ponder-sdk build",
    "consumer:dev": "pnpm --filter consumer dev",
    "consumer:build": "pnpm --filter consumer build",
    "consumer:start": "pnpm --filter consumer start"
  },
  "engines": {
    "node": ">=18.14",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.4"
}
